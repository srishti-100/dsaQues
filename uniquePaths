#include <bits/stdc++.h> 
/*
void helper(int r, int c, int rows, int cols, int& count){
	if(r<0 || r==rows|| c<0 || c==cols){
		return;
	}
	if(r == rows-1 && c ==cols-1){
		count++;
		return ;
	}

	helper(r+1,c, rows,cols,count);
	helper(r,c+1,rows,cols,count);

}
int uniquePaths(int m, int n) {
	// Write your code here.
	int count = 0;
	helper(0,0,m,n,count);
	return count;
}
*/

/*
int helper(int r, int c, int rows, int cols, vector<vector<int>>& dp){
	if(r<0 || r==rows|| c<0 || c==cols){
		return 0;
	}
	if(r == rows-1 && c ==cols-1){
		return 1;
	}

	if(dp[r][c]!=-1) return dp[r][c];

	int ans1 = helper(r+1,c,rows,cols,dp);
	int ans2 = helper(r,c+1,rows,cols,dp);

	dp[r][c] = ans1+ans2;

	return dp[r][c];

}

int uniquePaths(int m, int n) {
	// Write your code here.
	vector<vector<int>> dp(m,vector<int>(n,-1));

	return helper(0,0,m,n,dp);
}
*/

